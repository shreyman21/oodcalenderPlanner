package model;

public class AnytimeSchedulingStrategy implements SchedulingStrategy {
  @Override
  public void scheduleEvent(Event event, Schedule schedule) {
    // Implementation for anytime scheduling
  }
}